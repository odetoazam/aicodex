NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Shared secret for POST /api/revalidate, used to flush ISR after a content
# update in Supabase. Generate with: openssl rand -hex 32
REVALIDATE_SECRET=
